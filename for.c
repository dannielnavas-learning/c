#include <stdio.h>

int main()
{
    for (int i = 0; i < 5; i++) // aplica cuando conocemos el tamaño del bucle de antemano se utiliza for para iterar arrays
    {
        printf("i = %d\n", i);
    }
    return 0;
}
